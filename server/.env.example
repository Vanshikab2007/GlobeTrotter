PORT=4000
# Set a long random value in any real deployment — the server falls back to a
# dev-only default if this is missing, which is fine for local hackathon demos only.
JWT_SECRET=change-me-to-a-long-random-string
